<%- include('partials/stock.ejs') %>

<script>
    const deleteRow= (btn)=>{
          if (confirm("are you sure to delete ALL product!!")) {

            const serialNumber = btn.value 
            const body = {
                serialNumber
            }
            const options = {
                    method: 'DELETE',
                    body: JSON.stringify(body),
                    headers: {
                        'Content-Type': 'application/json'
                        }
                    }
                    console.log(options, name + "from stock")
                    fetch('/deleteproduct', options).then((res) =>{
                        const tr = btn.parentNode.parentNode
                        const tbody = tr.parentNode
                        tr.style.display ='none'
                    } ).catch((error) => console.log(error));
        }
    }

</script>